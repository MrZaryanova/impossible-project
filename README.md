# impossible-project

The goal of the project will be to create an NES emulator in a low level programming language with only one import (an import for graphics and audio). The emulator should be accurate enough to run Super Mario Bros.
